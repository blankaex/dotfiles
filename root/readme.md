The files in this directory will not be deployed by `stow`.

I think it is generally bad practice to symlink files from user directories to root directories.

There are not many files here and they change infrequently so I would recommend installing them manually if you want them.
